[project]
name = "metall-calculator-desktop"
version = "0.1.0"
description = "Калькулятор веса металлопроката для сметных расчетов"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
